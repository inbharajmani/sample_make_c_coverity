#include <header.h>
#include <stdio.h>
#include <math.h>

int main()
{
	//check the correct count for command line argument
	// if(argc < 3)
	// {
	// 	fprintf(stderr, "Enter correct i/p");
	// 	return FAILURE;
	// }

	// int line_count = 0,
	//     i = 0;
	// char **file_line = NULL;  
	// char *file_string = NULL;

	// line_count = count_file_lines(argv[2]);					//to find the total count of words in a line
	// file_line = read_file(argv[2]);						//to read the lines in a text
	// file_string = file_process(file_line, argv[1], line_count);		//to add suffix at the end of the text
	// write_file(file_string);		   				//to display the final string

	// //free memory
	// for(i = 0; i < line_count; i++)
	// {
	// 	free(file_line[i]);
	// 	file_line[i] = NULL;
	// }
	
	// if(file_line)
	// {
	// 	free(file_line);
	// 	file_line = NULL;
	// }

	// //free memory
	int file_string=0;
	if(file_string)
	{
		printf("test");
	}
	printf("test");
	return SUCCESS;
}

